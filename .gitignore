# APM dependencies (restored by `apm install`)
apm_modules/

# APM-compiled harness outputs (regenerated by `apm compile` / `apm install`)
/AGENTS.md
/CLAUDE.md
/.agents/
/.github/instructions/
/.github/prompts/

# Skill run artifacts (when this repo is itself measured)
.analysis/

# gograph index
.gograph/

# Python / tooling caches
__pycache__/
*.pyc
.ruff_cache/

# OS / editor
.DS_Store
*.swp
*~
